# Filter rows with missing data: `bio_NA`

After the addition of bins in the `bio_bin` step rows were removed if they were missing important data. A row was removed if it had the value `NA` in any important columns: `r envFunc::vec_to_sentence(bin)`.
