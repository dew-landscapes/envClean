# Flora attributes

Every record required an estimate of cover for use in the floristic clustering (also see [flora attributes](#attributes) and [PCA Groups](#pcagroups)). If no estimate of cover could be determined for a record, that record was removed.
