### Geographic density

Sites that occurred closer together than `r 1000 * settings_clean$this_dist` m were thinned using the `thin_points` function from the `GeoThinneR` package `r cite_package("GeoThinneR")`.
