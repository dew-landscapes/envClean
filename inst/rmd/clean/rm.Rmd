# Spatial reliability: `bio_rm`

Duplicate spatial reliability values per taxa within a bin were summarised by taking the minimum non-NA value (i.e. if a context had reliability values of 1000, 100 & NA, they would be reduced to 100). This was handled by the `reduce_geo_rel` function in *envClean* `r cite_package("envClean")`.
