# Start of the cleaning process: `bio_all`

The dataset that the cleaning process was applied to was the `bio_all` object created in the `envOcc` project. In this case it contained `r format(clean_summary$records[clean_summary$name == "bio_all"], big.mark = ",")` records.
